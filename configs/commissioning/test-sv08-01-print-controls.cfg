# Inactive upstream print interfaces for a separately selected private machine
# configuration. This file is not a printing configuration by itself.
# Include once only after the persistent gcodes directory exists and the
# private configuration has been checked for duplicate section ownership.
# PAUSE does not shut off heaters; RESUME may move and execute arbitrary G-code.
# Bare CANCEL_PRINT does not guarantee heater, fan or host-stream shutdown.
# H01, H02 and H05 precede any H06 control use. Motion, heat and print macros
# require their own later H06 commissioning evidence.
[virtual_sdcard]
path: /run/sv08/printer_data/gcodes

[pause_resume]

[display_status]

[respond]
